# Runtime schemas — S1–S2

P0 uses TypeScript contracts, not runtime JSON schemas. S1 adds versioned
engineering-record validation/persistence; S2 adds safe structured-model schemas
and evaluation. Type checking and descriptor registration do not establish
student readiness, physical validity or successful verification.
